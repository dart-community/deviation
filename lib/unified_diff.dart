/// Generate the unified diff between two lists of strings,
/// often representing the contents of two files.
@experimental
library;

import 'package:meta/meta.dart';

export 'src/unified_diff.dart';
